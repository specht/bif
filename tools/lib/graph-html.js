const { sourceLocation } = require("./source-links");

function safeJson(value) {
  return JSON.stringify(value).replace(/</g, "\\u003c").replace(/>/g, "\\u003e").replace(/&/g, "\\u0026").replace(/\u2028/g, "\\u2028").replace(/\u2029/g, "\\u2029");
}

function withSourceLocations(model) {
  const copy = JSON.parse(JSON.stringify(model));
  for (const node of copy.nodes) if (node.source) {
    node.source.location = sourceLocation(node.source);
  }
  for (const edge of copy.edges) {
    const source = { file: edge.file, line: edge.line, column: edge.column };
    edge.location = sourceLocation(source);
  }
  return copy;
}

function generateHtml({ model, svg, projectRoot }) {
  const data = safeJson(withSourceLocations(model));
  return `<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>${escapeHtml(model.project.title)} · Story authoring graph</title>
<style>
:root{color-scheme:light dark;--bg:#f6f7f9;--panel:#fff;--text:#20242a;--muted:#667085;--line:#d0d5dd;--accent:#2457c5;--accent-soft:#dbe7ff;--error:#b42318;--warning:#9a6700;--node:#eef3fb;--missing:#fee4e2}*{box-sizing:border-box}html,body{height:100%;margin:0;overflow:hidden}body{font:14px/1.45 system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;background:var(--bg);color:var(--text)}button,input,select{font:inherit;color:inherit}button,select,input{border:1px solid var(--line);border-radius:5px;background:var(--panel);padding:.42rem .6rem}button{cursor:pointer}button:focus-visible,input:focus-visible,select:focus-visible,a:focus-visible{outline:3px solid var(--accent);outline-offset:2px}.app{height:100%;display:grid;grid-template-rows:auto 1fr}.toolbar{display:flex;align-items:center;gap:.5rem;flex-wrap:wrap;padding:.65rem .8rem;border-bottom:1px solid var(--line);background:var(--panel)}h1{font-size:1rem;margin:0 .6rem 0 0}.summary{color:var(--muted);margin-right:auto}.search-wrap{display:flex;align-items:center;gap:.35rem}.search-wrap input{width:15rem}.layout{min-height:0;display:grid;grid-template-columns:minmax(0,1fr) 22rem}.canvas{position:relative;overflow:hidden;touch-action:none;background:var(--bg);cursor:grab}.canvas.dragging{cursor:grabbing}.canvas svg{width:100%;height:100%;display:block}.canvas svg .graph{transform-origin:center;transition:transform .15s ease}.authoring-cluster>polygon{pointer-events:none}.details{overflow:auto;border-left:1px solid var(--line);background:var(--panel);padding:1rem}.details h2{margin-top:0;font-size:1.1rem}.details h3{font-size:.9rem;margin:1.2rem 0 .4rem}.details dl{display:grid;grid-template-columns:auto 1fr;gap:.3rem .7rem}.details dt{font-weight:600}.details dd{margin:0;overflow-wrap:anywhere}.diagnostic{border-left:4px solid var(--warning);padding:.45rem .6rem;margin:.5rem 0;background:color-mix(in srgb,var(--warning) 9%,transparent)}.diagnostic.error{border-color:var(--error)}.source{display:flex;gap:.4rem;align-items:center;flex-wrap:wrap}.location{font-family:ui-monospace,SFMono-Regular,Consolas,monospace}.choice{border-top:1px solid var(--line);padding:.55rem 0}.authoring-node polygon,.authoring-node path{fill:var(--node)!important;stroke:#667085!important}.authoring-node.start polygon,.authoring-node.start path{stroke-width:2.5!important}.authoring-node.unreachable polygon,.authoring-node.unreachable path{stroke-dasharray:6 4}.authoring-node.has-error polygon,.authoring-node.has-error path,.authoring-node.missing polygon,.authoring-node.missing path{stroke:var(--error)!important;fill:var(--missing)!important}.authoring-node.has-warning polygon,.authoring-node.has-warning path{stroke:var(--warning)!important}.authoring-edge.conditional path{stroke-dasharray:6 4}.authoring-edge.broken path,.authoring-edge.broken polygon{stroke:var(--error)!important}.authoring-node,.authoring-edge{cursor:pointer}.selected polygon,.selected path{stroke:var(--accent)!important;stroke-width:3!important}.search-match polygon,.search-match path{stroke:#7c3aed!important;stroke-width:3!important}.filtered{opacity:.07;pointer-events:none}.match-count{min-width:4rem;color:var(--muted)}.badge{display:inline-block;border:1px solid var(--line);border-radius:999px;padding:.08rem .4rem;margin-right:.25rem}.empty{color:var(--muted)}
@media(max-width:800px){.layout{grid-template-columns:1fr 18rem}.search-wrap input{width:10rem}}@media(prefers-color-scheme:dark){:root{--bg:#15181d;--panel:#20242a;--text:#edf0f4;--muted:#a8b0bd;--line:#3f4752;--accent:#86a9ff;--accent-soft:#263b68;--node:#2b3545;--missing:#552622;--error:#ff8a80;--warning:#ffd166}.authoring-node text,.authoring-edge text,.authoring-cluster text{fill:var(--text)!important}}@media(prefers-reduced-motion:reduce){*,*::before,*::after{scroll-behavior:auto!important;transition-duration:.001ms!important;animation-duration:.001ms!important}}
</style>
</head>
<body>
<main class="app">
<header class="toolbar">
<h1>${escapeHtml(model.project.title)} · Authoring graph</h1>
<span class="summary">${model.summary.pages} pages · ${model.summary.edges} choices · ${model.summary.errors} errors · ${model.summary.warnings} warnings</span>
<label class="search-wrap">Search <input id="search" type="search" autocomplete="off"><span id="match-count" class="match-count" aria-live="polite">0 matches</span></label>
<label>Status <select id="status-filter"><option value="all">All pages</option><option value="reachable">Reachable</option><option value="unreachable">Unreachable</option><option value="errors">Errors</option><option value="warnings">Warnings</option><option value="missing">Missing targets</option></select></label>
<label>Group <select id="group-filter"><option value="all">All groups</option>${model.groups.filter(group=>group.groupId!=="cluster-missing").map(group=>`<option value="${escapeHtml(group.name)}">${escapeHtml(group.name)}</option>`).join("")}</select></label>
<button id="zoom-in" type="button" aria-label="Zoom in">+</button><button id="zoom-out" type="button" aria-label="Zoom out">−</button><button id="fit" type="button">Fit</button><button id="reset" type="button">Reset</button>
</header>
<div class="layout"><div id="canvas" class="canvas" data-view-state="1,0,0">${svg}</div><aside id="details" class="details" aria-live="polite"><h2>Story overview</h2><p>Select a page or choice to inspect source details.</p><p><span class="badge">${model.summary.unreachablePages} unreachable</span><span class="badge">${model.summary.missingTargets} missing targets</span></p></aside></div>
</main>
<script id="graph-data" type="application/json">${data}</script>
<script>
(()=>{'use strict';const data=JSON.parse(document.getElementById('graph-data').textContent);const byId=id=>document.getElementById(id);const canvas=byId('canvas'),details=byId('details'),search=byId('search'),statusFilter=byId('status-filter'),groupFilter=byId('group-filter');const graph=canvas.querySelector('g.graph'),baseTransform=graph.getAttribute('transform')||'';let scale=1,x=0,y=0,selected=null,matches=[],matchIndex=-1;const h=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const locationHtml=item=>item.location?'<div class="source"><span class="location">'+h(item.location)+'</span><button class="copy-location" type="button" data-location="'+h(item.location)+'">Copy location</button></div>':'';const diagnosticsHtml=list=>list.length?'<h3>Diagnostics</h3>'+list.map(d=>'<div class="diagnostic '+h(d.severity)+'"><strong>'+h(d.severity.toUpperCase()+' · '+d.code)+'</strong><br>'+h(d.message)+'</div>').join(''):'';
function applyView(){graph.setAttribute('transform',baseTransform+' translate('+x+' '+y+') scale('+scale+')');canvas.dataset.viewState=[scale.toFixed(3),Math.round(x),Math.round(y)].join(',')}function choose(item){if(selected)byId(selected)?.classList.remove('selected');selected=item.nodeId||item.edgeId;byId(selected)?.classList.add('selected');if(item.kind==='page')showPage(item);else if(item.kind==='missing')showMissing(item);else showEdge(item)}
function showPage(node){const outgoing=data.edges.filter(e=>e.source===node.pageId);details.innerHTML='<h2>Page '+h(node.pageId)+'</h2><dl><dt>Path</dt><dd>'+h(node.path)+'</dd><dt>Group</dt><dd>'+h(node.group||'None')+'</dd><dt>Graph label</dt><dd>'+h(node.graphLabel||'None')+'</dd><dt>Reachability</dt><dd>'+h(node.reachable?'Reachable':'Unreachable')+'</dd><dt>Links</dt><dd>'+node.incomingCount+' incoming, '+node.outgoingCount+' outgoing</dd><dt>Content</dt><dd>'+node.counts.scripts+' scripts, '+node.counts.conditions+' conditions, '+node.counts.expressions+' expressions, '+node.counts.images+' images</dd></dl>'+locationHtml(node.source||{})+diagnosticsHtml(node.diagnostics)+'<h3>Outgoing choices</h3>'+(outgoing.length?outgoing.map(e=>'<div class="choice"><button type="button" data-edge="'+h(e.edgeId)+'">'+h(e.text||e.target)+'</button><div>Target: '+h(e.target)+(e.label?' · Label: '+h(e.label):'')+(e.condition?' · Condition: '+h(e.condition):'')+'</div><span class="location">'+h(e.location)+'</span></div>').join(''):'<p class="empty">No outgoing choices.</p>');bindDetails()}
function showEdge(edge){details.innerHTML='<h2>Choice '+h(edge.source)+' → '+h(edge.target)+'</h2><dl><dt>Text</dt><dd>'+h(edge.text||'None')+'</dd><dt>Graph label</dt><dd>'+h(edge.label||'None')+'</dd><dt>Condition</dt><dd>'+h(edge.condition||'None')+'</dd><dt>Broken target</dt><dd>'+h(edge.broken?'Yes':'No')+'</dd></dl>'+locationHtml(edge)+diagnosticsHtml(edge.diagnostics);bindDetails()}
function showMissing(node){const incoming=data.edges.filter(e=>e.target===node.pageId);details.innerHTML='<h2>Missing: '+h(node.pageId)+'</h2><dl><dt>Expected file</dt><dd>'+h(node.path)+'</dd><dt>Incoming broken references</dt><dd>'+incoming.length+'</dd></dl>'+diagnosticsHtml(node.diagnostics)+'<h3>References</h3>'+incoming.map(e=>'<div class="choice"><button type="button" data-edge="'+h(e.edgeId)+'">'+h(e.source+': '+e.text)+'</button><br><span class="location">'+h(e.location)+'</span></div>').join('');bindDetails()}
function bindDetails(){details.querySelectorAll('[data-edge]').forEach(button=>button.onclick=()=>choose(data.edges.find(e=>e.edgeId===button.dataset.edge)));details.querySelectorAll('.copy-location').forEach(button=>button.onclick=()=>navigator.clipboard?.writeText(button.dataset.location))}
function searchable(item){return [item.pageId,item.filename,item.path,item.graphLabel,item.group,item.text,item.label,item.condition,...(item.diagnostics||[]).map(d=>d.message)].filter(Boolean).join(' ').toLowerCase()}function updateSearch(){document.querySelectorAll('.search-match').forEach(e=>e.classList.remove('search-match'));const q=search.value.trim().toLowerCase();matches=q?[...data.nodes,...data.edges].filter(item=>searchable(item).includes(q)):[];matches.forEach(item=>byId(item.nodeId||item.edgeId)?.classList.add('search-match'));byId('match-count').textContent=matches.length+' match'+(matches.length===1?'':'es');matchIndex=-1}
function updateFilters(){const state=statusFilter.value,group=groupFilter.value;const visible=new Set();for(const node of data.nodes){const okState=state==='all'||state==='reachable'&&node.kind==='page'&&node.reachable||state==='unreachable'&&node.kind==='page'&&!node.reachable||state==='errors'&&node.errorCount>0||state==='warnings'&&node.warningCount>0||state==='missing'&&node.kind==='missing';const okGroup=group==='all'||node.group===group;byId(node.nodeId)?.classList.toggle('filtered',!(okState&&okGroup));if(okState&&okGroup)visible.add(node.nodeId)}for(const edge of data.edges)byId(edge.edgeId)?.classList.toggle('filtered',!(visible.has(edge.sourceNodeId)&&visible.has(edge.targetNodeId)))}
for(const edge of data.edges){const group=byId(edge.edgeId),path=group?.querySelector('path');if(path){const hit=path.cloneNode(false);hit.removeAttribute('id');hit.setAttribute('class','edge-hit');hit.setAttribute('stroke','transparent');hit.setAttribute('stroke-width','14');hit.setAttribute('fill','none');hit.setAttribute('pointer-events','stroke');path.parentNode.insertBefore(hit,path)}}for(const node of data.nodes)byId(node.nodeId)?.addEventListener('click',event=>{event.stopPropagation();choose(node)});for(const edge of data.edges)byId(edge.edgeId)?.addEventListener('click',event=>{event.stopPropagation();choose(edge)});search.addEventListener('input',updateSearch);search.addEventListener('keydown',event=>{if(event.key==='Enter'&&matches.length){event.preventDefault();matchIndex=(matchIndex+1)%matches.length;choose(matches[matchIndex])}else if(event.key==='Escape'){search.value='';updateSearch()}});document.addEventListener('keydown',event=>{if(event.key==='Escape'&&document.activeElement!==search){if(selected)byId(selected)?.classList.remove('selected');selected=null;details.innerHTML='<h2>Story overview</h2><p>Select a page or choice to inspect source details.</p>'}});statusFilter.onchange=updateFilters;groupFilter.onchange=updateFilters;
byId('zoom-in').onclick=()=>{scale=Math.min(4,scale*1.2);applyView()};byId('zoom-out').onclick=()=>{scale=Math.max(.2,scale/1.2);applyView()};byId('reset').onclick=()=>{scale=1;x=0;y=0;applyView()};byId('fit').onclick=()=>{const box=graph.getBBox(),rect=canvas.getBoundingClientRect();scale=Math.max(.2,Math.min(2,Math.min(rect.width/(box.width+40),rect.height/(box.height+40))));x=0;y=0;applyView()};canvas.addEventListener('wheel',event=>{event.preventDefault();scale=Math.max(.2,Math.min(4,scale*(event.deltaY<0?1.1:.9)));applyView()},{passive:false});let drag=null;canvas.addEventListener('pointerdown',event=>{if(event.target.closest('.authoring-node,.authoring-edge'))return;drag={x:event.clientX,y:event.clientY,ox:x,oy:y};canvas.classList.add('dragging');canvas.setPointerCapture(event.pointerId)});canvas.addEventListener('pointermove',event=>{if(!drag)return;x=drag.ox+event.clientX-drag.x;y=drag.oy+event.clientY-drag.y;applyView()});canvas.addEventListener('pointerup',()=>{drag=null;canvas.classList.remove('dragging')});applyView();updateFilters();})();
</script>
</body></html>`;
}

function escapeHtml(value) {
  return String(value ?? "").replace(/[&<>"']/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[character]));
}

module.exports = { generateHtml, safeJson, withSourceLocations };
