<!-- Async fixture -- Suspended script -->

# Async navigation fixture

The source passage is mounted before its script finishes.

<script>
    const answer = await presentChoice([
        ['inspect', 'Inspect the machine'],
        ['leave', 'Leave it alone'],
    ]);
    resumed_answer = answer;
    print(`Continuation resumed with **${answer}**.`);
    await goToPage('2');
</script>

<div condition="false">

- [Graph-only destination](2)

</div>

