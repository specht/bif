<!-- Keyboard fixture -- Scripted choices -->

## Scripted choice passage

<script>
    const answer = await presentChoice([
        ['accept', 'Accept the temporary choice'],
        ['reject', 'Reject the temporary choice'],
    ]);
    keyboard_answer = answer;
    print(`Keyboard continuation: **${answer}**`);
    await goToPage('6');
</script>

<div condition="false">

- [Graph-only programmatic destination](6)

</div>

